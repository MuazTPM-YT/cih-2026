//! `telemed-gw` — workspace root package.
//!
//! This crate is intentionally empty: it exists only to host the workspace-level
//! integration test in `tests/` (see `tests/lossy_delivery.rs`). All real logic lives in
//! the member crates under `crates/`.
